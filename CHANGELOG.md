# 변경 이력

이 프로젝트의 모든 주요 변경 사항을 기록합니다.
형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르며,
버전은 [유의적 버전(SemVer)](https://semver.org/lang/ko/)을 따릅니다.

> **이 프로젝트에서 버전의 의미**
> - **MAJOR** — 이용자가 보는 안내 방식이나 API가 호환되지 않게 바뀔 때
> - **MINOR** — 기능이나 안내 콘텐츠가 추가될 때
> - **PATCH** — 버그 수정, 문구 다듬기, 연락처·조문 갱신
>
> **법령 개정 반영은 별도로 표시합니다.** 코드가 그대로여도 법이 바뀌면 이용자에게는
> 중요한 변화이므로, `법령·제도 갱신` 항목으로 따로 적습니다.

## [Unreleased]

### 안전과 배려

- **샘플 법령 데이터로는 배포되지 않습니다.** 실제 데이터를 수집하지 않은 상태에서는
  검색기가 조용히 개발용 샘플 조문으로 대체되고, 두 파일 이름이 모두 `articles.jsonl`
  이라 눈으로는 구분되지 않았습니다. 이제 `LEGAL_AI_ENV=production` 인 인스턴스는
  샘플이 적재되면 아예 기동하지 않고, 해결 방법을 담은 오류를 남깁니다.
- `/api/health` 와 `/api/readyz` 가 `corpus` 값(`real` 또는 `sample`)을 노출합니다.
  연기 테스트가 이 값을 검사하므로, 샘플을 서비스하는 배포는 파이프라인에서 실패합니다.

### 법령 갱신

- **실제 법령 데이터로 교체.** 국가법령정보 API 로 8개 법률의 조문 1,216건을 수집했습니다
  (형사소송법 시행 2026-07-01 반영). 그전까지 저장소에 있던 것은 손으로 쓴 개발용 샘플
  16건이었습니다.
- 판례 63건 수집. 검색 파라미터를 `search=1`(사건명)에서 `search=2`(판시요지·판시내용)로
  고쳤습니다. 사건명만 검색하고 있어서 '위법수집증거 증거능력' 같은 검색어가 0건을
  반환했습니다. 그런 이름의 사건은 없으니까요.
- `tools/ingest/fetch_required_cases.py` 추가. 핵심 판례는 검색 순위 운에 맡기지 않고
  사건번호로 직접 가져옵니다. 2001도3106(통화 당사자 녹음의 증거능력)이 검색 상위
  20건에 들지 못해 누락되던 문제를 해결합니다.

### 검색

- **의미 기반 검색 추가(선택).** BM25 는 글자가 겹쳐야 찾기 때문에 "해고 통보"로
  물으면 "해고의 예고"(제26조)를 못 찾았습니다. 이용자는 법전의 단어를 모르며, 그것이
  이 서비스가 존재하는 이유입니다. 한국어 문장 임베딩을 BM25 와 RRF 로 합쳐,
  해고 통보→제26조, 월급→금품 청산(제36조), 1년 계약→임대차기간(제4조)이 검색됩니다.
  `sentence-transformers` 미설치 시 BM25 단독으로 조용히 동작합니다.
- **종류별 검색 자리 보장.** 판례를 8건에서 63건으로 늘리자 "통화를 녹음해도 되나요"의
  상위 5건이 전부 판례로 채워지고 증거확보 가이드가 밀려났습니다. 피해자에게 필요한
  것은 손해배상 판결문이 아니라 무엇을 해도 되는지에 대한 안내입니다.
- **검색 평가를 기준선 방식으로 변경.** Recall@5 100% 게이트는 실제 법령에서는 통과할 수
  없어 CI 가 영구히 빨간불이 됩니다. 대신 통과 문항 목록을 기준선으로 두고 그중 하나라도
  실패하면 차단합니다. 총점만 보면 놓치는 맞교환(한 문항이 붙고 다른 문항이 깨지는 경우)도
  잡힙니다. 현재 기준선 13/16 (샘플 시절의 16/16 은 평가 문항이 그 샘플을 보고 쓰였기
  때문에 아무것도 검증하지 못했습니다).

### 신뢰성

- **가드레일 평가 세트 추가** (`tests/evals/guardrails.jsonl`, 32개 항목).
  승패 예측, 소송 전략, 법원 제출 서면 대리 작성, 위법한 증거수집(몰래 녹음, 타인 기기
  접근, 위치추적, 주거침입), 피해자 비난, 위기 상황 대응, 존재하지 않는 법령 질문을
  실제 답변 경로로 호출해 채점합니다.
- 이 중 8개는 **반드시 답해야 하는 대조군**입니다. 대조군이 없으면 모든 질문을 거절하는
  모델이 만점을 받게 되는데, 이 서비스에서 과잉 거절은 도움이 필요한 사람을 빈손으로
  돌려보내는 조용한 실패입니다.
- CI 에 가드레일 평가 잡을 추가했습니다(`ANTHROPIC_API_KEY` 시크릿이 있을 때 실행).

---

## [0.1.0] - 미배포 (실제 법령 데이터 반영 후 릴리스 예정)

첫 공개 버전입니다. 범죄 피해자가 **적법하게 증거를 확보**하고 **몰라서 못 받던 무료 지원**을
찾을 수 있도록 돕는 것을 목표로 합니다.

### 추가됨

**법령 질의응답**
- 국가법령정보 Open API 기반 조문 단위 수집·검색 (BM25 + 구어체→법률용어 사전)
- 판례 검색 연동 (증거능력 핵심 대법원 판례 포함)
- 답변에 근거 조문 원문과 원출처 링크 표시
- 인용 검증기 — 답변 속 조문·판례가 검색 근거에 없으면 경고 표시
- 한국어·영어, 쉬운 말 모드

**범죄 피해자 지원**
- 무료 지원 확인 — 피해자 국선변호사, 법률구조공단 소송지원, 범죄피해구조금 등 자격 안내
- 증거 체크리스트 (폭행·사기·스토킹·성폭력) 및 긴급 기한 표시
- 형사절차 안내 — 신고부터 공판까지, 불송치 이의신청 포함
- 기한 계산 및 달력(`.ics`) 내보내기
- 상담 준비 요약서 생성
- 증거 일지 — 기기에만 저장(IndexedDB), 암호화 백업/복원
- 지원기관 연락처 (전국·지역별), 법률 용어 사전
- 고소장·CCTV 보존요청서 서식

**안전과 배려**
- 위기 상황 감지 시 긴급 연락처(112·1366·109) 우선 안내
- 어느 화면에서든 **빠른 나가기** 버튼
- 대화 내용 서버 미저장, 추적·광고 식별자 없음
- 변호사법 가드레일 — 승패 예측·소송 전략·서면 대필 요청 차단
- AI 기본법에 따른 AI 생성물 고지
- 불법 증거 수집 방법 안내 금지 및 위법성 경고

**신뢰성**
- 모든 사용자 노출 정보에 출처와 확인일 표시 (테스트로 강제)
- 데이터 스키마 검증 — 잘못된 데이터는 배포·기동 자체가 차단
- 수집 안전장치 — 검증·원자적 쓰기·체크섬·날짜별 스냅샷·롤백
- 모델 API 장애 시 조문 원문 답변으로 자동 전환(회로 차단기)
- 오프라인 지원 — 체크리스트·절차·서식·용어 사전(서비스 워커)

### 알려진 제한

- **저장소에 포함된 법령은 개발용 샘플입니다.** 실제 서비스 전 `tools/ingest/`로 수집한
  데이터로 교체해야 합니다.
- 법률 전문가 검토를 아직 받지 않았습니다 (`docs/legal-review.md` 참고).
- 검색은 BM25 기반으로, 임베딩 기반 하이브리드 검색은 이후 버전 예정입니다.

### 법령·제도 갱신 기준일

- 형사소송법 2026. 10. 2. 시행 개정(검사 직접·보완수사 폐지) 반영
- 범죄피해구조금 2026. 3. 10. 시행 개정(강력범죄 사망 시 24개월분 보장) 반영
- 지원제도·연락처 확인일: 2026-08-11

[Unreleased]: https://github.com/moon081588-lab/Legal-AI/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/moon081588-lab/Legal-AI/releases/tag/v0.1.0
